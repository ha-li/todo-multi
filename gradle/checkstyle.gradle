apply plugin: 'checkstyle'

ext.checkstyleConfigDir = "$configDir/checkstyle"

checkstyle {
   toolVersion = '5.6'
   configFile = new File (checkstyleConfigDir, 'sun.checks.xml')
   ignoreFailures = true
   showViolations = false
}